{
	"extends": "./tsconfig.json",
	"include": [
		// Repeated from base config's "include" setting.
		"src",
		"cypress",
		"*.ts",
		"*.tsx",

		// These are the eslint-only inclusions. When a TS parsing error is thrown, add affected file here.
		".eslintrc.cjs",
		"tailwind.config.js"
	]
}
